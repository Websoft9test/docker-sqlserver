# image: https://hub.docker.com/r/bitnami/suitecrm
# docs: https://github.com/bitnami/bitnami-docker-suitecrm


version: '3.8'

services:
  db:
    image: mcr.microsoft.com/mssql/server:${APP_DB_VERSION}
    container_name: ${APP_NAME}
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=${APP_PASSWORD}
      - MSSQL_PID: ${APP_PID}
    ports:
      - ${APP_DB_PORT}:1433
    volumes:
      #- mssql_data:/var/lib/mysql
    restart: always

networks:
  default:
      name: ${APP_NAME}

volumes:
  mssql_data:
